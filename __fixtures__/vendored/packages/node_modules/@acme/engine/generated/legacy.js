// Re-excluded by the fixture's own .gitignore, so it must stay out of the index
// even though its parent directory was put back in.
class LegacyEngine {
  ignite() {
    return 'generated';
  }
}

module.exports = LegacyEngine;
